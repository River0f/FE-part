import styled from "@emotion/styled";
import { Button } from "@mui/material";

export const PrimaryCustomButton = styled(Button)`
  background: var(--violet-dark);
  width: 120px;
  margin-bottom: 20px;
  &:hover {
    background: var(--violet-dark);
  }
`;
